[package]
name = "leadership-homer"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.8", features = ["csr"] }
console_error_panic_hook = "0.1"

[profile.release]
opt-level = "z"
lto = true
